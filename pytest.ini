[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra

markers =
    smoke: fast critical-path tests
    regression: broader functional regression tests
    visual: visual validation tests
    negative: negative-path and error-handling tests